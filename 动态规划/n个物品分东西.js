/**
 * 
 */
function getMaxValue(T,n,vals){
    /**
     * 思路：感觉还是dp算法（但是这个思路可能有问题，可能取不到最大值）
     */
    function dp(vals,i){
        // dp表示小明取到的最大值，i表示当前小明在数组中的位置
        // 每次计算数组中没有被计算的最大值
        
    }
}